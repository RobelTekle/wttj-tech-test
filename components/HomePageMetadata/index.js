import Head from 'next/head'

const PageMetadata = () => (
  <Head>
    <title>Welcome to the Jungle</title>
    <meta name="description" content="WTTJ Jobs" />
    <link rel="icon" href="/favicon.ico" />
  </Head>
)

export default PageMetadata
